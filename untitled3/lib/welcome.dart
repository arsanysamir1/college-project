import 'package:flutter/material.dart';
class wlcome extends StatelessWidget {
  const wlcome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Column(children: [],)),);
  }
}
